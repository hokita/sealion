---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: sealion
  labels:
    app: sealion-frontend
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30010
      protocol: TCP
      name: http
  selector:
    app: sealion-frontend
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sealion-frontend
  namespace: sealion
  labels:
    app: sealion-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sealion-frontend
  template:
    metadata:
      labels:
        app: sealion-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/hokita/sealion-fe:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3000"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      restartPolicy: Always
